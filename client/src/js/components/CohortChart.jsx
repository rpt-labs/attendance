// component to show attendance data in chart form by cohort
// clicking will take to new cohort component

import React from 'react';

const CohortChart = props => {
  return <div>This is a cohort chart component to show the overall health of the cohort</div>;
};

export default CohortChart;
